// apps/mobile/android/build.gradle (nivel de proyecto)
plugins {
  id("com.android.application") version "8.0.2" apply false
  // gracias a includeBuild(...) en settings.gradle no hace falta versión aquí:
  id("com.facebook.react") apply false
  // si usas Kotlin, descomenta:
  // id("org.jetbrains.kotlin.android") version "1.9.22" apply false
}

// NADA de buildscript{} ni dependencies{ classpath(...) } aquí

subprojects {
  repositories {
    google()
    mavenCentral()
    // Repos de RN en monorepo
    maven { url("$rootDir/../../node_modules/react-native/android") }
    maven { url("$rootDir/../node_modules/react-native/android") }
  }
}
