{
  "name": "novelreader-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @novel/core build",
    "dev:desktop": "npm run -w desktop dev",
    "build:desktop": "npm run -w desktop tauri build",
    "start:mobile": "npm run -w mobile start",
    "android": "npm run -w mobile run-android"
  },
  "dependencies": {
    "@novel/core": "^0.0.1",
    "jszip": "^3.10.1"
  }
}
